# ============================================================
# AI Voice Language Tutor — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
#
# Note: ffmpeg must be installed separately as a system tool.
# Download from: https://ffmpeg.org/download.html
# ============================================================

# --- Web Framework ---
fastapi>=0.111.0          # Modern async Python web framework
uvicorn[standard]>=0.30.0 # ASGI server to run FastAPI
python-multipart>=0.0.9   # Required for file uploads in FastAPI

# --- Groq SDK (used for BOTH Whisper STT and LLM) ---
groq>=0.9.0               # Official Groq Python SDK

# --- Text-to-Speech (Microsoft Edge TTS — completely free, no API key) ---
edge-tts>=6.1.10          # Async TTS using Microsoft's Edge voices

# --- Configuration ---
python-dotenv>=1.0.0      # Load .env file into environment variables
pydantic>=2.7.0           # Data validation for API request/response schemas

# --- Testing ---
pytest>=8.0.0             # Test runner
pytest-asyncio>=0.23.0    # Async test support
httpx>=0.27.0             # HTTP client used to test FastAPI endpoints
